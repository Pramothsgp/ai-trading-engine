# Data Processing Configuration
# Maximum number of parallel workers for data processing tasks
DATA_MAX_WORKERS=4

# Sleep time (in seconds) between starting threads to avoid overlap/conflicts
DATA_THREAD_SLEEP=0.1
